#include "../include/indTask/greek.h"

int main(int argc, char *argv[]) {
    if (argc > 0) {
        for (int i = 0; i < argc; i++) {
            //добавить в массив аргументы
        }
    }
    else {
        //scanf()
    }
    return 0;
}
/*
    int output;
    char v[] = "V";
    output = number_feeder(v);
    if (digit('V') == output){
        return (1 == 1);
    }
    */